/* Encounter screen — single-scroll form (Phase B).
 *
 * The page tab strip was removed in favor of one continuously-
 * scrolling form, so the page-switching IIFE that used to live here
 * has been deleted. Future encounter-level interactions (status
 * bar updates, scroll-spy if it ever comes back, document-label
 * tabs that repurpose the old tab strip DOM) attach here.
 */
