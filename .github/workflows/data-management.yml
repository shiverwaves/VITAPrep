# Unified data management workflow: inventory existing data or extract new
# distributions from Census PUMS microdata.
#
# Two modes:
#   - inventory: Scan data/ and report which tables exist, row counts, and
#                completeness per extraction part (P1/P2/P3).
#   - extract:   Download PUMS files for the given state(s)/year, run the
#                extraction pipeline, and optionally open a PR with the
#                resulting SQLite file(s).
#
# Dispatch-only — choose what you want to do via the action input.

name: Data Management

on:
  workflow_dispatch:
    inputs:
      action:
        # "inventory" audits existing SQLite files in data/ without downloading
        # anything.  "extract" downloads PUMS data and builds distribution
        # tables for the selected state(s), year, and parts.
        description: "inventory = audit existing data  |  extract = download PUMS & build tables"
        required: true
        default: "inventory"
        type: choice
        options:
          - inventory
          - extract
      states:
        # One or more U.S. state abbreviations.  Each state runs as a
        # parallel matrix job during extraction.  Ignored for inventory.
        description: "State(s) to extract — comma-separated FIPS abbreviations (extract only)"
        required: false
        default: "HI"
        type: string
      year:
        # The ACS 5-Year PUMS release year.  Census publishes annually;
        # 2022 is the latest release with full coverage.
        description: "ACS 5-Year PUMS release year (extract only)"
        required: false
        default: "2022"
        type: string
      parts:
        # Part 1 = demographics/household structure (12 tables)
        # Part 2 = employment/income (12 tables)
        # Part 3 = housing/deductions (3 tables)
        # Space-separated; e.g. "1 2 3" extracts all, "3" extracts only Part 3.
        description: "Which extraction parts to run: 1=demographics  2=income  3=expenses"
        required: false
        default: "1 2 3"
        type: string
      create_pr:
        # When true, the workflow commits the SQLite file(s) to a new branch
        # and opens a PR.  Set to false for dry-run extraction without PR.
        description: "Open a PR with the extracted SQLite file(s) (extract only)"
        required: false
        default: true
        type: boolean
      format:
        # Controls how the inventory report is rendered in the job summary.
        # "markdown" is best for GitHub Actions; "json" for scripted consumers.
        description: "Inventory output format (inventory only)"
        required: false
        default: "markdown"
        type: choice
        options:
          - markdown
          - text
          - json

jobs:
  # ── Inventory existing data ────────────────────────────────────────
  inventory:
    name: Data inventory
    if: inputs.action == 'inventory'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Run data inventory
        run: |
          FORMAT="${{ inputs.format || 'markdown' }}"
          python scripts/data_inventory.py \
            --format "$FORMAT" \
            --github-summary

      - name: Print inventory to logs
        run: |
          echo "=== Distribution Data Inventory ==="
          python scripts/data_inventory.py --format text
          echo "==================================="

  # ── Parse states into a matrix ─────────────────────────────────────
  prepare-matrix:
    name: Prepare extraction matrix
    if: inputs.action == 'extract'
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - name: Parse state list into matrix
        id: set-matrix
        run: |
          STATES="${{ inputs.states }}"
          JSON=$(echo "$STATES" | tr ',' '\n' | sed 's/^[[:space:]]*//' | sed 's/[[:space:]]*$//' | tr '[:lower:]' '[:upper:]' | jq -R . | jq -s -c .)
          echo "matrix={\"state\":$JSON}" >> "$GITHUB_OUTPUT"
          echo "Matrix: {\"state\":$JSON}"

  # ── Extract distribution data ──────────────────────────────────────
  extract:
    name: Extract ${{ matrix.state }}
    if: inputs.action == 'extract'
    needs: prepare-matrix
    runs-on: ubuntu-latest
    timeout-minutes: 30
    strategy:
      matrix: ${{ fromJson(needs.prepare-matrix.outputs.matrix) }}
      fail-fast: false

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install extraction dependencies
        run: |
          pip install pandas numpy sqlalchemy requests

      - name: Cache PUMS downloads
        uses: actions/cache@v4
        with:
          path: extraction/pums_cache
          key: pums-${{ matrix.state }}-${{ inputs.year }}

      - name: Run extraction
        run: |
          python -m extraction.extract_all \
            --state "${{ matrix.state }}" \
            --year "${{ inputs.year }}" \
            --parts ${{ inputs.parts }} \
            --verbose

      - name: Validate output
        run: |
          STATE_LOWER=$(echo "${{ matrix.state }}" | tr '[:upper:]' '[:lower:]')
          DB_FILE="data/distributions_${STATE_LOWER}_${{ inputs.year }}.sqlite"

          if [ ! -f "$DB_FILE" ]; then
            echo "ERROR: Expected output file not found: $DB_FILE"
            exit 1
          fi

          echo "Validating $DB_FILE..."
          python -c "
          import sqlite3, sys
          conn = sqlite3.connect('$DB_FILE')
          cursor = conn.cursor()
          cursor.execute(\"SELECT name FROM sqlite_master WHERE type='table'\")
          tables = [r[0] for r in cursor.fetchall()]
          print(f'Tables found: {len(tables)}')
          for t in tables:
              cursor.execute(f'SELECT COUNT(*) FROM [{t}]')
              count = cursor.fetchone()[0]
              print(f'  {t}: {count} rows')
              if count == 0:
                  print(f'  WARNING: {t} is empty!')
          conn.close()
          if len(tables) < 1:
              print('ERROR: No tables found')
              sys.exit(1)
          print('Validation passed')
          "

      - name: Run data inventory
        run: |
          python scripts/data_inventory.py --format markdown --github-summary

      - name: Upload SQLite artifact
        uses: actions/upload-artifact@v4
        with:
          name: distributions-${{ matrix.state }}-${{ inputs.year }}
          path: data/distributions_*.sqlite
          retention-days: 90

      - name: Upload extracted database for PR
        if: inputs.create_pr
        uses: actions/upload-artifact@v4
        with:
          name: db-${{ matrix.state }}-${{ inputs.year }}
          path: data/distributions_*.sqlite

  # ── Create PR with extracted data ──────────────────────────────────
  create-pr:
    name: Create PR
    if: inputs.create_pr && (inputs.action == 'extract')
    needs: extract
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install inventory dependencies
        run: pip install pandas numpy sqlalchemy

      - name: Download all extracted databases
        uses: actions/download-artifact@v4
        with:
          pattern: db-*
          path: data/
          merge-multiple: true

      - name: List downloaded files
        run: ls -la data/distributions_*.sqlite 2>/dev/null || echo "No files found"

      - name: Generate inventory summary
        id: inventory
        run: |
          SUMMARY=$(python scripts/data_inventory.py --format markdown)
          echo "summary<<EOF" >> "$GITHUB_OUTPUT"
          echo "$SUMMARY" >> "$GITHUB_OUTPUT"
          echo "EOF" >> "$GITHUB_OUTPUT"

      - name: Create PR branch and commit
        run: |
          BRANCH="data/extract-${{ inputs.states }}-${{ inputs.year }}-$(date +%Y%m%d-%H%M%S)"
          git checkout -b "$BRANCH"
          git add data/distributions_*.sqlite
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git commit -m "Extract distribution data: ${{ inputs.states }} (${{ inputs.year }})"
          git push -u origin "$BRANCH"
          echo "BRANCH=$BRANCH" >> "$GITHUB_ENV"

      - name: Create Pull Request
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh pr create \
            --title "Extract distribution data: ${{ inputs.states }} (${{ inputs.year }})" \
            --body "$(cat <<'PREOF'
          ## Distribution Data Extraction

          **States:** ${{ inputs.states }}
          **Year:** ${{ inputs.year }}
          **Parts:** ${{ inputs.parts }}

          ### What this PR does
          Extracts PUMS distribution tables from Census ACS 5-Year microdata and adds
          the resulting SQLite file(s) to `data/`.

          ### Data Summary
          ${{ steps.inventory.outputs.summary }}

          ### How to verify
          1. Download the artifact from the extraction workflow run
          2. Open the SQLite file and inspect table contents
          3. Run `python scripts/data_inventory.py` locally after merging

          ---
          *Generated by the Data Management workflow.*
          PREOF
          )"
